/**
 * Rust lets you split a package into multiple crates and a crate into modules so you can refer to items defined in one module from another module. 
 * Rust允许我们将一个package拆分为多个Crate，并将一个Crate拆分为多个模块。
 */
pub mod benzi{
        pub fn drive(){
           println!("今天提一台普尔曼");
       }
   }